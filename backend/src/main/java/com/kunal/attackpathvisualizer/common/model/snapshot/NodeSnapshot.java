package com.kunal.attackpathvisualizer.common.model.snapshot;

/** Snapshot DTO for a Kubernetes Node (cluster-scoped). Maps to core.model.Node. */
public class NodeSnapshot extends ResourceSnapshot {
    public NodeSnapshot() {}
}
