package com.kunal.attackpathvisualizer.common.model.snapshot;

/** Snapshot DTO for a Kubernetes ConfigMap. Maps to core.model.ConfigMap. */
public class ConfigMapSnapshot extends ResourceSnapshot {
    public ConfigMapSnapshot() {}
}
