package com.kunal.attackpathvisualizer.common.model.snapshot;

/** Snapshot DTO for a Kubernetes ServiceAccount. Maps to core.model.ServiceAccount. */
public class ServiceAccountSnapshot extends ResourceSnapshot {
    public ServiceAccountSnapshot() {}
}
